pipeline {
    agent any

    stages {
        stage('Clone') {
            steps {
                git branch: 'main',
                    url: 'https://github.com/mmuthukrishnan2003/backend.git'
            }
        }

        stage('Build') {
            steps {
                bat 'dir'
            }
        }
    }
}
